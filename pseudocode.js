if (count < oneMinuslengthOfguessedWord ){
	inquirer.prompt([
      {
        name: "word",
        message: "choose a letter"
      } 
}then(answers => {
  var guessedLetters = [];

});

//store few words in a collection
var collectionOfWords = ["word1", "word2", "word3"];
//access a random word from the collectionOfWords array and store it
var randomWord = Math.random();

// log a random word to the terminal
console.log(randomWord);

// display the random word as underscores insted of letters

// store the underscore that we are going to be accessing
var underScore = "_";

// split the randomWord string as an array
var randomWordLetters = .split()randomWord;

//replace the underScores as far as the length of the word
for (i = 0, i<randomWordLetters.length, i++){
	var displayUnderscore = randomWordLetters.replace()underScore;
	displayUnderscore += displayUnderscore

}

